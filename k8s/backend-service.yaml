apiVersion: v1
kind: Service
metadata:
  name: lms-backend-service
  namespace: default
spec:
  selector:
    app: lms-backend
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 8080   
  type: ClusterIP