apiVersion: v1
kind: Service
metadata:
  name: user105-back-service
  namespace: default
spec:
  selector:
    app: user105-back
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
